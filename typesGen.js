// use graphql-types-generator@1.1.1 and later insted of thos

// function generateType(inputFields) {
// 	JSON.stringify(inputFields.reduce((acc, [k, v]) => ((acc[k] = typeMatches[v] || 'any'), acc),{}))
// 	.replace(/"/g, '').replace(/\:/g, ': ')
// }